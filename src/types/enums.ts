export enum Language {
  EN = 'en',
  UA = 'uk',
}

export enum ThemeVariants {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum RoutesPath {
  HOME = '/',
  ANALYTICS = '/analytics',
  SETTING = '/setting',
  CALENDAR = '/calendar',
}

export enum LocalStorageKeys {
  LOCALE = 'locale',
  THEME = 'theme',
}
